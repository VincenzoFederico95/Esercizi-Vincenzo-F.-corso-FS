/*Perform a code refactoring by using the Rest operator. The output of the console.log must not change.*/


const person = { id, ...personInfo } = {
    id: 1,
    firstName: "Mario",
    lastName: "Rossi",
    age: 25,
  };
  
  console.log(id, personInfo);

  